# Debug Log

Failed approaches and their outcomes. Append here — never retry what's already logged.

## Notes

- ClickUp v2 API does NOT expose doc content. Use v3: `GET /api/v3/workspaces/{workspace_id}/docs` and `GET /api/v3/workspaces/{workspace_id}/docs/{doc_id}/pages`
